{
    "name": "typescript",
    "version": "1.0.0",
    "devDependencies": {
      "typescript": "^2.7.2"
    },
    "scripts": {
      "start": "tsc && node build/index.js",
      "prestart": "npm run build",
      "build": "tsc"
    }
}